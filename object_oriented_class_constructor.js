
//creating a class with the name of 'hello'
class hello{
  
  constructor(name,roll) //parameterized constructor 
  {
      console.log("Name is : "+ name+ " and roll is : "+ roll);
  }
}
//creating object of hello class as 'h1' using following code of lines 
let h1=new hello('kavi',20);

//when object of a class is created then constructor is called.
//if we do not provide any constructor then default constructor is provided by javascript.

